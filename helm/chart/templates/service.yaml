apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-cloud-devsecops.fullname" . }}
  labels:
    {{- include "multi-cloud-devsecops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-cloud-devsecops.selectorLabels" . | nindent 4 }}
